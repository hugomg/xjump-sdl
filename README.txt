# Dependencies

    sudo dnf install SDL2-devel SDL2_ttf-devel

# Features

- More responsive controls (left->right vs right->left jumps)
- Game can now be resized and stretched
- UI is more symmetrical
